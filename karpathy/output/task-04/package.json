{
  "name": "karpathy-task-04-clean-contact-records",
  "version": "1.0.0",
  "private": true,
  "description": "Robust contact record transformation with validation, normalization, and deduplication (karpathy task-04).",
  "main": "src/functions/cleanContactRecords.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "typescript": "^5.4.5"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["<rootDir>/src/**/*.test.ts"]
  }
}
