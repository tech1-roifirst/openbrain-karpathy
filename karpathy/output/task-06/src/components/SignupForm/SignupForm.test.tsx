/**
 * The Playwright suite is the source of truth for SignupForm coverage. It
 * lives at `tests/signup.spec.ts` (the standard Playwright location) and runs
 * against the production preview build via `npm run test:e2e`.
 *
 * This file exists so a reviewer searching the component folder for tests is
 * pointed to the right location. See:
 *   - tests/signup.spec.ts        (Playwright E2E — covers all 10 success criteria)
 *   - playwright.config.ts        (desktop + mobile projects)
 */
export {};
